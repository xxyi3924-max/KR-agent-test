# Agent Trader Dependencies

# Core
PyYAML>=6.0
requests>=2.28.0
pyyaml>=6.0
pytest>=7.0.0

# Optional: for better data handling
numpy>=1.24.0

# Optional: for scheduled runs (included in stdlib)
# threading - built-in

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
